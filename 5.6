//Задача: Даны три равноразмерные матрицы. Необходимо в соответствующей ячейке третьей матрицы записывать большие элементы из первой и второй
//матриц соответствующих ячеек
#include <iostream>
#include <cstdlib>
#include <cmath>

using namespace std;
int a = -1000,b=1000;
int main()
{
    setlocale(LC_ALL,"rus");
    cout <<"Введите размерность матрицы (n x k)"<<endl;
    int n,k;
    cin >>n;
    cin >>k;

    int matrix1[n][k];
    int matrix2[n][k];
    int matrix3[n][k];
    int massiv_matrix[k*n];
    int index=0;

    for (int i =0; i<n;i++)
    {
        for (int j =0; j<k; j++)
        {
            matrix1[i][j]=a+rand()%((b+1)-a);
            cout <<matrix1[i][j]<<' ';
        };
        cout<<endl;
    };
    cout <<endl;

    for (int i =0; i<n;i++)
    {
        for (int j =0; j<k; j++)
        {
            matrix2[i][j]=a+rand()%((b+1)-a);
            cout <<matrix2[i][j]<<' ';
        };
        cout<<endl;
    };
    cout <<endl;

    for (int i =0; i<n; i++)
    {
        for (int j =0; j<k; j++)
        {
            if (matrix1[i][j]>matrix2[i][j])
            {
                matrix3[i][j]=matrix1[i][j];
            }
            else
            {
                matrix3[i][j]=matrix2[i][j];
            };
        };
    };
    for (int i =0; i<n;i++)
    {
        for (int j =0; j<k; j++)
        {
            cout <<matrix3[i][j]<<' ';
        };
